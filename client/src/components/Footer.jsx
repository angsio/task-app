const styles = {
    bar: 'bg-violet-700',
}

export const Footer = () => {
    return (
        <div className={`h-1/20 w-full flex items-center justify-center ${styles.bar}`}>
            Footer
        </div>
    )
}